import { StringAlteratePipe } from './string-alterate.pipe';

describe('StringAlteratePipe', () => {
  it('create an instance', () => {
    const pipe = new StringAlteratePipe();
    expect(pipe).toBeTruthy();
  });
});
